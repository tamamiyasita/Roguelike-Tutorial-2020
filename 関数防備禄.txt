random.choice(list)
リストからランダムに一つの要素を取り出す

hasattr(obj, str(属性名)) -> bool
オブジェクト内の属性の有無を調べる

exec("x=12; y=20; print(x,y)") -> 12, 20
文を実行する。変数や属性の設定などができる

eval("10+20") -> 30
計算式などを実行して返す

format文字の詰め書式
txt = "ABC"
print(f"{txt: >4}"で空白を一つ置いて右詰めの書式になる<で左＾で中央
